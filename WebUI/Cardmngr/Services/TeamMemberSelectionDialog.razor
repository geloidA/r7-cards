@code {
    private static readonly RenderFragment<UserInfo> RenderUser = user =>
        @<div class="flex flex-row py-2 items-center">
            <div class="ms-1">
                <UserAvatar User="user"/>
            </div>
            <span class="ms-2">
                @user.DisplayName
            </span>
        </div>;
}
