<KolBoard TValue="Feedback" Class="m-1" CanItemDrag="x => x.CanChangeStatus">
    <Columns>
        <FeedbackColumn Status="FeedbackStatus.Todo">
            <HeaderTemplate>
                <div class="d-flex w-100">
                    <div class="col align-self-center">
                        Предложения 📝
                    </div>
                    <div class="col d-flex justify-content-end mt-1">
                        <Button Color="ButtonColor.Dark" Size="Size.ExtraSmall"
                            @onclick="OpenCreateModalAsync" >
                            <Icon Name="IconName.Lightbulb"/> Добавить
                        </Button>
                    </div>
                </div>
            </HeaderTemplate>
        </FeedbackColumn>

        <FeedbackColumn Status="FeedbackStatus.InProgress">
            <HeaderTemplate>
                <div style="margin-top: 1px;">В работе 👨‍💻</div>
            </HeaderTemplate>
        </FeedbackColumn>

        <FeedbackColumn Status="FeedbackStatus.Finished">
            <HeaderTemplate >
                <div style="margin-top: 1px;">Готово 🏁</div>
            </HeaderTemplate>
        </FeedbackColumn>
    </Columns>
    <ItemTemplate Context="feedback">
        <FeedbackCard Feedback="feedback" />
    </ItemTemplate>
</KolBoard>
