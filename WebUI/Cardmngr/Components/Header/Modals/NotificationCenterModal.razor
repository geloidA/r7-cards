@inject IMessageService MessageService

<Offcanvas Width="324"
    Class="my-offcanvas my-offcanvas-right" Placement="MyOffcanvasPlacement.End" >
    <OffcanvasHeader>
        <span class="fw-bold fs-4 user-select-none">
            Уведомления
        </span>
    </OffcanvasHeader>
    <OffcanvasBody Class="d-flex flex-column overflow-auto">        
        <FluentMessageBarProvider MaxMessageCount="int.MaxValue" Type="MessageType.Notification" Section="@App.MESSAGES_NOTIFICATION_CENTER" />
    </OffcanvasBody>
</Offcanvas>

@code {
    [Parameter] public object? Nothing { get; set; }
}
